# neomacs-aur

Standalone automated AUR sync runner for [`neomacs-bin`](https://aur.archlinux.org/packages/neomacs-bin).

Monitors [`eval-exec/neomacs`](https://github.com/eval-exec/neomacs) releases and automatically updates the Arch User Repository (AUR) package on every new release.
